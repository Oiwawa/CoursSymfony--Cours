<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Accueil</title>
</head>
<body>
<p>Page home</p>
{# voici un commentaire en twig #}

<p>J'affiche du text {{ 'variableDynamique' }} </p>
<p>J'affiche la date {{ "now"|date('d/m/Y') }}</p>

{# Une boucle entre {% %}#}
<ul>
{% for i in 0..10  %}
{# {{ i }} pour afficher la variable   #}
 <li>  {{ i }}</li>
{% endfor %}
{# termine la boucle for   #}
</ul>

<ul>

    {% set max = 15%}
    {%  for i in 0..max  %}
        <li>  {{ i }}</li>
    {% endfor %}
</ul>
</body>
</html>