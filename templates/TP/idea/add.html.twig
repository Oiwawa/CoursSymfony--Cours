{% extends 'TP/layout.html.twig' %}

{% block titre %}

{% endblock %}
{% block menu %}

    <div class="container-fluid">
        <div class="row">
            <div class="my-5 d-flex justify-content-center">
                <h3>Ajouter une nouvelle idée à votre Bucket-List</h3>
            </div>
            <div class="col-md-12 my-3 d-flex justify-content-center">
                <ul class="nav nav-pills">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('TP_index') }}">Accueil</a>
                    </li>
                    <a class="nav-link" href="{{ url('TP_contact') }}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('TP_aboutus') }}">A propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('TP_liste') }}">Liste</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('TP_add') }}">Nouvelle idée</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
{% block main %}
    <div class="container-fluid">
        <div class="row">
            <div class="my-5 d-flex justify-content-center">
                <div>
                    {{ form_start(ideaForm) }}
                    {{ form_widget(ideaForm) }}
                    <br>
                    <button class="btn btn-outline-dark" type="submit">Ajouter idée</button>
                    {{ form_end(ideaForm) }}
                </div>
                <br>
            </div>
        </div>
    </div>
{% endblock %}

